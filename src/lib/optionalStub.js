// Returned by the Metro resolver hook (metro.config.js) when one of the
// optional packages listed in OPTIONAL_MODULES isn't installed. optionalRequire
// treats a null module as "not available" and the caller no-ops.
module.exports = null;
